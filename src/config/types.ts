export enum database {
  type = 'DATABASE_TYPE',
  host = 'DATABASE_HOST',
  port = 'DATABASE_PORT',
  username = 'DATABASE_USERNAME',
  password = 'DATABASE_PASSWORD',
  name = 'DATABASE_NAME',
}

export enum server {
  port = 'SERVER_PORT',
  protocol = 'SERVER_PROTOCOL',
  host = 'SERVER_HOST',
}
